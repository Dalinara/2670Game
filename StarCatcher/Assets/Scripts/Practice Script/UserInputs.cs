using UnityEngine;
using System.Collections;

public class UserInputs : MonoBehaviour 
{


	// Use this for initialization
	void Start () 
	{
	
	}
	
	// Update is called once per frame
	void Update () 
	{
		//The player should be moving using the arrows, and the 'w','s','a', and 'd' keys.
		//Jump should be the space bar.
		//'v' + Mouse drag should move the camera
	}
}
