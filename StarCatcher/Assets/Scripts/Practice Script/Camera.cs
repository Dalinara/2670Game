using UnityEngine;
using System.Collections;

public class Camera : MonoBehaviour 
{
	public GameObject cam;
	// Use this for initialization
	void Start () 
	{
	
	}

	void OnMouseDrag()
	{
		//When the user presses 'v'(in UserInputs) and drags the mouse it should move the camera's rotation, only to a certain point. 
		//The y transform should have  alimit for up and down.
//		transform.position  
	}

}
